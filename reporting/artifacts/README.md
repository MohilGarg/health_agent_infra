# Canonical checked-in artifacts root

This directory is the sole canonical checked-in proof root for the repo.

Reviewer rule for this checkpoint:
- use `reporting/artifacts/` for checked-in proof review
- keep new checked-in proof bundles under this root or another explicit bucket-native proof surface
- do not recreate a repo-root `artifacts/` tree as a second live proof root

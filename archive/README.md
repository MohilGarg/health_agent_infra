# Archive

This directory holds repo surfaces that are intentionally kept out of the live flagship Health Lab review path.

Current archived lane:
- `legacy_product_surfaces/`
  - `dashboard/`
  - `web/`
  - `garmin/`

These surfaces are retained for history and possible later reference, but they are not part of the current canonical `health_model/`-centered proof path.
